package 객체;

public class 생성자 {

	//생성자 매소드
	/*
	 * 
	 * 인스턴스가 생성될떄마다 호출되는 인스텅스 초기화 메서드
	 * 인스턴스 변수의 초기화 또는 인스턴스 생성시 수행할 작업에 사용
	 * 모든 클래스에는 반드시 하나 이상의 생성자가 있어야 한다.
	 * 인스턴스 초기화 - 인스턴스 변수에 적절한 값을 저장하는 것
	 * 생성자의 조건 - 클래스의 이름과 같아야 한다
	 * 리턴값이 없다
	 * 기본 생성자가 하나도 없으면 컴파일러가 기본으로 하나 생성한다
	 * 
	 * 메소드 오버로딩
	 * 
	 * 명시적 초기화는 값을 지정해주는것
	 * 생성자 메소드를 사용해 초기화 해주는것
	 * 초기화 블럭-잘 사용하지 않음
	 * - 인스턴스 초기화블럭{}
	 * 클래스  초기화 블럭 static{}
	 * 
	 * 
	 * 
	 * 
	 */
	
	
	
}
