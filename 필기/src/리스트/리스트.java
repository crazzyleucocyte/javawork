package 리스트;

import java.util.ArrayList;


public class 리스트 {
	/*
	 * 리스트에 객체를 추가 할때는 add메서드를 사용해서 맨 끝에 들어간다 반환형은boolean형으로 들어가면 트루가 된다
	 * void add는 중간에 끼워넣기
	 * set은 이미 있는 객체를 바꾸기
	 * get은 가져오기
	 * isEmpty는 해당 컬랙션이 비어있나?
	 * size는 배열에서 랭스
	 * clear는 모든 객체 삭제
	 * E remove(index)는 해당 인덱스를 삭제
	 * boolean remove(Object)는 해당 객체를 지우고 지워졌으면 트루 반환
	 * 
	 * 
	 * 
	 * 
	 * 
	 */
	
//	public class Buyer {
//		int money=500;
//		int bonusPoint;
//		ArrayList<객체> 리스트 이름= new  ArrayList<객체>();
		//객체를 만들어서 쓸떄 사용하는게 리스트<>에 객체를 넣는다
		//<>에는 어떤 객체가 들어와도 된다라는 뜻이다
		//ArrayList는 객체를 생성하는것이다.
	//리스트에 추가할떄는 
//	리스트 이름.add(넣을 객체);
//	로 사용한다
	
}
